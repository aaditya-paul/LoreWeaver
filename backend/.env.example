# Copy this file to backend/.env and fill in your API keys before running Docker.
# Required — the backend will refuse to start without GROQ_API_KEY.

# ── LLM API Keys ───────────────────────────────────────────────────────────────
GROQ_API_KEY=gsk_your_groq_key_here
GOOGLE_API_KEY=AIza_your_google_key_here

# ── Local LLM (Ollama) ─────────────────────────────────────────────────────────
# Set to true if you want to use a local model via Ollama.
# When running inside Docker, leave OLLAMA_URL as-is (set in docker-compose.yml).
LOCAL_LLM_ENABLED=false
# OLLAMA_URL=http://host.docker.internal:11434

# ── Auth / Security ────────────────────────────────────────────────────────────
# Random secret used to sign JWT tokens. Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change_me_to_a_random_secret_string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080
